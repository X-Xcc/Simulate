# 脚本模块

## 功能说明
便捷的批处理脚本，用于快速启动、构建和部署系统。

## 脚本清单

| 脚本名 | 功能 |
|--------|------|
| **start_all.bat** | 一键启动所有服务 (Java后端 + Qwen服务 + YOLOv8监控) |
| **build_war.bat** | 构建WAR包 |
| **deploy.bat** | 部署应用 |
| **install_qwen_deps.bat** | 安装Qwen模型依赖 |

## 使用方法
```bash
# 启动所有服务
start_all.bat

# 仅构建
build_war.bat

# 部署
deploy.bat

# 安装Qwen依赖
install_qwen_deps.bat
```

## 说明
- 这些脚本是Windows批处理文件
- 需要在项目根目录执行
- 确保已安装Java和Python环境
