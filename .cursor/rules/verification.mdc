---
description: "Evidence before assertions — must run verification commands before claiming done"
alwaysApply: true
---

## 验证铁律

```
没有新鲜的验证证据，不许宣称完成
```

未运行验证命令 → 不能声称"测试通过""修复完成""没问题"。
使用"应该""大概""似乎" → 停下来，运行验证。
验证前就表达满意（"搞定！""完美！"）→ 停下，先验证。

## 门控函数

宣称任何状态之前：
1. **确定** — 什么命令能证明这个结论？
2. **运行** — 执行完整命令（重新运行，完整执行）
3. **阅读** — 完整输出，检查退出码，统计失败数
4. **验证** — 输出是否支持结论？否→报告实际状态。是→带证据陈述
5. **只有这时** — 才能做出结论

跳过任何一步 = 说谎，不是验证。

## 常见失败模式

| 结论 | 需要 | 不够格 |
|------|------|--------|
| 测试通过 | `0 failures` 的输出 | 之前的运行结果、"应该会通过" |
| 构建成功 | 构建命令 `exit 0` | linter 通过、"看起来没问题" |
| Bug 已修复 | 原始症状测试通过 | "代码改了，应该修好了" |
| 需求已满足 | 逐项核对清单 | "测试通过了" |

## 项目验证命令

| 场景 | 命令 |
|------|------|
| TypeScript 编译 | `npx tsc --noEmit` |
| 前端构建 | `npm run build` |
| Java 编译 | `.\mvnw compile` |
| Java 测试 | `.\mvnw test` |
| Python 测试 | `pytest -v` |

修改 `server/` 代码后必须重建 WAR + 确认服务正常。
修改 `DetectionService` 相关 → 确认 `invalidateScanCache()` 已调。

## 红线

- "应该能行了" → 运行验证命令
- "我有信心" → 信心 ≠ 证据
- "Linter 通过了" → Linter ≠ 编译器
- "部分检查就够了" → 部分检查什么也证明不了
