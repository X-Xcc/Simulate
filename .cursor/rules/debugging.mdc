---
description: "Systematic debugging — root cause before fix, 4-stage process"
alwaysApply: true
---

## 系统调试铁律

```
不做根因调查，不许提修复方案
```

随意修复 = 浪费时间 + 引入新 bug。草率补丁 = 掩盖深层问题。

## 四阶段流程

### 第一阶段：根因调查

**在尝试任何修复之前：**

1. **仔细阅读错误信息** — 完整堆栈跟踪、行号、文件路径、错误码
2. **稳定复现** — 可靠触发？具体步骤？无法复现→收集数据，不猜测
3. **检查近期变更** — git diff、新依赖、配置变更、环境差异
4. **多组件系统** — 沿边界逐层诊断，记录进出数据，确定断裂点在哪一层
5. **跟踪数据流** — 错误值从哪产生？谁传进来的？持续向上追踪到源头。在源头修复，不是在症状处

### 第二阶段：模式分析

1. 找到可正常工作的类似代码（同一代码库）
2. 逐行对比参考实现和出问题代码
3. 列出每一个差异——不假设"那不可能有影响"
4. 理解依赖关系：需要哪些组件、设置、环境？

### 第三阶段：假设与验证

1. 提出单一假设："我认为 X 是根因，因为 Y"
2. 最小化测试——每次只改一个变量
3. 生效→进第四阶段；没生效→新假设
4. 不确定时说"我不理解 X"，不假装知道

### 第四阶段：实施

1. 创建失败的测试用例（复现 bug）
2. 实施单一修复——每次只改一处
3. 验证：测试通过？其他测试没破坏？
4. **如果修复不起作用：**
   - 少于 3 次 → 回第一阶段
   - **3 次或以上 → 停下来质疑架构**。每次修复暴露新问题 = 架构有误。和用户讨论，不继续修补

## 红线

以下想法出现 → 停下来，回第一阶段：
- "先临时修一下，以后排查"
- "试着改改 X 看看"
- "大概是 X 的问题，修一下"
- "我不完全理解，但这应该能行"
- 没有追踪数据流就提解决方案

## 本项目特殊规则

- 修改 `DetectionService` 相关 → 确认 `invalidateScanCache()` 已调
- 修改 JSON 文件读写 → 确认 ReadWriteLock 未被绕过
- 修改前端 API → 确认 30s 缓存策略兼容
