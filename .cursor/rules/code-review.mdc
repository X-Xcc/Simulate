---
description: "Code review standards — Chinese style review, feedback response patterns"
alwaysApply: false
---

## 代码审查规范

### 审查顺序（优先级从高到低）
1. **架构** — 方案是否合理？有没有更好的方式？
2. **正确性** — 逻辑对不对？边界条件处理了吗？
3. **安全性** — 注入？越权？信息泄露？
4. **性能** — N+1 查询？内存泄漏？不必要的循环？
5. **可维护性** — 半年后能看懂吗？测试覆盖了吗？
6. **风格** — 只关注工具无法自动处理的部分

### 反馈分级

| 级别 | 含义 | 说明 |
|------|------|------|
| **[必须修复]** | 安全漏洞、数据丢失、逻辑错误 | 不修不能合并 |
| **[建议修改]** | 性能问题、可维护性、缺少校验 | 本次或下次迭代修复 |
| **[仅供参考]** | 命名优化、风格建议、替代方案 | 不改也行 |
| **[问题]** | 不确定的地方 | 需要作者解释意图 |

### 反馈模板
```
[必须修复] SQL 注入风险

第 42 行：用户输入直接拼接到 SQL 语句中。

原因：攻击者可通过 name 参数注入恶意 SQL。

建议：使用参数化查询：
  db.query('SELECT * FROM users WHERE name = $1', [name])
```

### 表达方式（中文团队）
- **用建议代替命令**："建议考虑用 X，因为 Y"而非"你必须改成 X"
- **用提问代替否定**："这里用 sync 方式读文件是出于什么考虑？"而非"不要用 sync"
- **先扬后抑**：好的实现要肯定，关键问题不因面子放过
- **Code Review 对事不对人**：技术正确性 > 社交舒适度

### 审查总结模板
```
总结：整体实现思路清晰，[优点]。

主要问题：
1. [必须修复] ...
2. [建议修改] ...
3. [仅供参考] ...

建议先修复 [必须修复] 的问题。
```

### 接收审查反馈
1. 完整阅读，理解后再行动
2. 不确定先澄清——不要部分理解就实施
3. 外部反馈先验证：对当前代码库正确吗？会破坏现有功能吗？
4. 确认正确的反馈：直接修，不敷衍附和（避免"你说得太对了！""好观点！"）
5. 反馈有误：用技术理由反驳，不带防御情绪

### 反模式
- 只写"LGTM"不说明审查了什么
- 纠结工具能自动处理的风格问题
- 所有评论委婉模糊，关键 bug 被埋没
- 因对方资历深就不敢提意见

### 本项目审查清单
- [ ] `AbstractJsonFileService<T>` 正确使用（原子写入、ReadWriteLock）
- [ ] 删除检测数据后调了 `invalidateScanCache()`
- [ ] 前端 API 缓存 key 覆盖到新请求
- [ ] SSE 订阅正确处理重连
- [ ] 认证路径配了 AuthFilter（公共路径例外）
