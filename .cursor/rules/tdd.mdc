---
description: "TDD iron law — no production code without a failing test first"
globs:
  - "*.ts"
  - "*.tsx"
  - "*.java"
  - "*.py"
alwaysApply: false
---

## TDD 铁律

```
没有失败的测试，就不写生产代码
```

先写了代码再写测试？删掉。从头来过。不要"留作参考"、不要"改编"、不要看一眼——删就是删。

### 为什么顺序重要

后写测试=立即通过。立即通过什么也证明不了：
- 可能测试了错误的东西
- 可能测试的是实现而非行为
- 可能遗漏了边界情况
- 你从未看到它捕获 bug

先写测试回答"代码应该做什么？"后写测试回答"代码做了什么？"——完全不同的两个问题。

## 红-绿-重构

### 红灯 — 编写失败的测试
- 一个行为、清晰名称、最小范围
- 使用真实代码（除非不得已才 mock）
- 名称中有"和"？拆分成两个测试

```typescript
// 好
test('空邮箱提交返回错误', async () => {
  const result = await submitForm({ email: '' });
  expect(result.error).toBe('邮箱不能为空');
});
```

### 验证红灯 — 必须执行
```bash
npm test path/to/test.test.ts  # 或其他测试命令
```
确认：测试失败（不是报错）、失败信息正确、因功能缺失而失败（不是拼写错误）
测试通过了？你在测试已有行为。修改测试。
测试报错了？修复错误重新跑。

### 绿灯 — 最少代码
写最简单的代码让测试通过。不要加功能、不要重构其他代码、不做"改进"。
```typescript
function submitForm(data: FormData) {
  if (!data.email?.trim()) return { error: '邮箱不能为空' };
  // ...
}
```

### 验证绿灯 — 必须执行
确认：测试通过、其他测试仍然通过、输出干净。

### 重构 — 只在绿灯后
消除重复、改善命名、提取辅助函数。保持绿灯。不添加行为。

### 重复
为下一个功能写下一个失败的测试。

## 常见借口与反驳

| 借口 | 现实 |
|------|------|
| "太简单了不用测" | 简单代码也会出 bug。测试只需 30 秒 |
| "之后补测试" | 立即通过的测试什么也证明不了 |
| "手动测试过了" | 临时测试≠系统测试。无记录，无法重现 |
| "删除 X 小时工作太浪费" | 沉没成本谬误。保留未验证代码=技术债 |
| "测试难写=设计不清楚" | 听测试的。难以测试=难以使用 |
| "TDD 会拖慢我" | TDD 比调试快。务实=先写测试 |

## 危险信号

以下任何一条出现 → 删除代码，TDD 从头来：
- 先写了代码再写测试
- 测试立即通过（没看到红）
- 无法解释测试为什么失败
- "之后再补"、"就这一次"、"已经手动测过了"

## 边界

- 不测试第三方库、简单 getter/setter、不可能发生的错误路径
- Bug 修复：写重现 bug 的失败测试 → TDD 循环
- 新功能：从测试开始
